#include <bits/stdc++.h>
using namespace std;


// Multi maps not genrally used in CP
int main()
{
    // In Multimap, multiple values can be stored for single keys
    multimap<int, int> m;

    // Many not use it and make vector<string> to store multiple value in normal map
    map<int, vector<string>> m1;
    return 0;
}